bundle:
  name: netcare-ml-platform
  engine: direct

include:
  - resources/*.yml

variables:
  catalog_name:
    description: Unity Catalog catalog used by this environment
    default: nectare
  experiment_name:
    description: Absolute Databricks workspace path for the MLflow experiment
    default: /Shared/netcare-readmission
  registered_model_name:
    description: Unity Catalog registered model name
    default: nectare.ml.readmission_model

# The training notebook imports the synced src package directly.
# No Python wheel is required for this notebook task, so deployments avoid
# the extra wheel build/upload step.
sync:
  paths:
    - ../src
    - ../notebooks

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://8259552034754034.4.gcp.databricks.com
    variables:
      catalog_name: nectare

  staging:
    mode: production
    workspace:
      host: ${DATABRICKS_HOST_STAGING}
    variables:
      catalog_name: nectare

  prod:
    mode: production
    workspace:
      host: ${DATABRICKS_HOST_PROD}
    variables:
      catalog_name: nectare
